Forms can be displayed on the frontend using the function

```php
BallDeep::form($id, $title = true)
```

where `$id` is the ID of the form and `$title` is whether or not you would like to display the title.